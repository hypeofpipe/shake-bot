export class Restaurant {
  name: string //"KFC Witosa"
  url: string //"kfc-witosa"
  minimal_quote: number //""
  constructor(name: string, url: string, minimal_quote: number) {
    this.name = name
    this.url = url
    this.minimal_quote = minimal_quote
  }
}
